[project]
name = "rnascraper"
version = "0.1.0"
description = "Automazione lettura aiuti RNA e sincronizzazione Google Sheets"
requires-python = ">=3.12"
dependencies = [
    "gspread>=6.0.0",
    "playwright>=1.60.0",
    "pandas>=2.0.0",
    "openpyxl>=3.1.0",
]

[dependency-groups]
dev = []

